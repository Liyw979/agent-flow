{
  "entry": "线索发现",
  "nodes": [
    {
      "type": "agent",
      "id": "线索发现",
      "prompt": " \
任务目标：分析当前目录下的 RFC 文件，找出当前项目实现与 RFC 规范不一致的地方，包括不限于默认行为与规范不一致、规范要求了当前项目没做、项目做了规范没写，特别是 RFC 明确要求禁止，但当前项目实现未禁止，或者采用了宽松校验、额外兼容、可能导致 fail-open 的地方 \
进度管理：进度记录在 `result/progress.md` 中；制定有条理的扫描策略，避免东找一个西找一个。每一轮都要记录当前分析到了哪个 RFC 章节。第一次分析时，需要先看 `result/progress.md` 的进展，再继续分析 \
任务要求：每次对话仅返回一个新的可疑 finding 并停止，避免一次返回多个 findings, 先更新progress.md再给出finding \
权限约束：不修改 progress.md 之外的文件 \
finding格式要求:\
  1. 可疑点标题\
  2. 违反的RFC要求（附上原文）\
  3. 实际的代码行为\
  4. 涉及文件与函数\
  5. 可能造成的影响\
回复要求（二选一）：\
- 当你找到一个新的可疑点时，先回复 <continue>，再接 finding 正文。\
- 当你确认当前项目里已经完全没有值得分析的不一致点时，先回复 <complete>，再接简短说明。这段说明至少包含\
    1. 历史finding的简介: 列表列出每个finding的描述、代码调用链\
    2. 你为什么认为当前项目没有值得分析新的可疑点\
- 当系统再次要求你继续工作时，你要继续寻找下一个新的 finding。",
      "writable": true
    },
    {
      "type": "spawn",
      "id": "疑点辩论",
      "graph": {
        "entry": "漏洞挑战",
        "nodes": [
          {
            "type": "agent",
            "id": "漏洞挑战",
            "prompt": "论证该 RFC 不一致不会导致安全漏洞、安全问题，不要从实际运行、实际影响的角度，并指出误报原因或存在约束条件。\
            要充分讨论，不能说未被证实 \
            回复格式（二选一）：\
            1. <continue> (从文档、RFC原文、SHOULD要求、代码、单元测试、不安全的场景、会被利用的场景、关键代码证据说明这不属于漏洞、不导致安全问题、反驳)... \
            2. <complete> (当多次讨论后，认同该 RFC 不一致会导致安全漏洞)... ",
            "writable": false
          },
          {
            "type": "agent",
            "id": "漏洞论证",
            "prompt": "从安全专家的角度给出属于漏洞理由，论证该 RFC不一致 会导致安全问题、安全漏洞，并持续补充论证。\
            回复格式（二选一）：\
            1. <continue> (从文档、RFC原文、MUST要求、代码、单元测试、不安全的场景、会被利用的场景、关键代码证据说明这属于漏洞、可能导致安全问题、反驳)... \
            2. <complete> (当多次讨论后，认同该 RFC 不一致不会导致安全漏洞)... ",
            "writable": false,
            "initialMessage": ["线索发现"],
          },
          {
            "type": "agent",
            "id": "讨论总结",
            "initialMessage": ["线索发现", "漏洞论证", "漏洞挑战"],
            "prompt": " \
在当前代码仓的 `result/<章节>_<finding标题>_<是否为漏洞>.md` 目录下写入讨论总结文件, 再输出一个概括。\
每个讨论单独一个文件；当前讨论必须新建新文件，不得覆盖、追加或复用已有讨论总结文件。\
需包含以下章节：\
1. 结论\
   - 给出一句话结论摘要\
   - 给出风险等级与影响面摘要 \
   - 不要从证据的角度，要从安全影响的角度分析，是否可能为漏洞 \
2. finding 描述\
   - 用 2 到 4 句话说明当前讨论的可疑点是什么\
   - 明确涉及的入口、关键参数、关键状态或资源\
3. 代码事实与证据链\
   - 列出支撑本次判断的关键文件、函数、调用链、判断分支、过滤条件、权限条件或数据流\
   - 每条证据都要写清“代码里的事实是什么”以及“这条事实支撑了什么判断”\
   - 明确区分“代码已直接证明”的事实与“根据现有事实做出的推断”\
4. 正方观点与成立条件\
   - 总结漏洞论证侧提出的核心主张\
   - 写清 RFC 不一致或实现偏差成立需要满足哪些前提，以及这些前提目前哪些已被代码证实、哪些还没有\
5. 反方观点与不成立理由\
   - 总结漏洞挑战侧提出的核心反驳\
   - 写清哪些保护逻辑、限制条件、调用约束、协议解释或上下文会削弱、阻断或推翻该偏差判断\
6. 争议点裁决\
   - 逐条列出双方仍有分歧的点\
   - 对每个分歧点明确给出你的裁决、裁决依据，以及为什么另一侧观点未能改变结论\
7. 当前已形成的稳定结论\
8. 仍未解决的不确定点\
9. 后续排查建议 ",
            "writable": true,
            "initialMessage": ["线索发现", "漏洞挑战", "漏洞论证"]
          }
        ],
        "links": [
          { "from": "漏洞论证", "to": "漏洞挑战", "trigger": "<continue>", "message_type": "last", "maxTriggerRounds": 4 },
          { "from": "漏洞挑战", "to": "漏洞论证", "trigger": "<continue>", "message_type": "last", "maxTriggerRounds": 4 },
          { "from": "漏洞论证", "to": "讨论总结", "trigger": "<complete>", "message_type": "last" },
          { "from": "漏洞挑战", "to": "讨论总结", "trigger": "<complete>", "message_type": "last" },
          { "from": "讨论总结", "to": "线索发现", "trigger": "<default>", "message_type": "none" }
        ]
      }
    },
    {
      "type": "agent",
      "id": "线索完备性评估",
      "prompt": "先看 `result/progress.md` 和 `result/` 目录下已经分析到了哪里，再基于已有结论继续评估当前项目的 RFC 不一致扫描是否已经足够完整。\
任务目标：判断当前项目是否还存在明显遗漏的 RFC 不一致方向，重点关注尚未覆盖的 RFC 章节、关键协议状态机、输入校验规则、默认行为、错误处理分支、兼容路径与可能导致 fail-open 的执行路径。\
进度管理：进度记录以 `result/progress.md` 为准；每一轮都要明确哪些 RFC 章节、实现模块、入口、状态流转已经被充分检查，哪些区域只是初步排除，哪些高风险方向仍缺少证据，不要重复回到已经充分覆盖的路径，也不要只凭印象给出“已经查完”的结论。\
进度核对要求：判断是否结束前，要按 RFC 章节、实现模块、入口、状态流转和风险类型检查 `result/progress.md` 与现有总结里的覆盖面，明确哪些区域已经被历史 finding 覆盖，哪些区域只是初步浏览，不能把“看过一遍”当成“已经充分排查”。\
轮次要求：如果发现仍有遗漏，当前轮只返回最值得继续补查的一项不一致点或补查方向，并停止等待下一轮。\
结论约束：只有在你能够明确说明“为什么现有排查已经覆盖了主要高风险 RFC 区域”时，才允许结束；不能因为暂时没有新 finding 就直接结束。\
回复格式要求：\
1. 若认为仍有遗漏，先回复 <continue>，再给出具体遗留的不一致点或补查方向。\
2. 若认为已经没有遗漏，先回复 <complete>，再给出简短结论。\
回答要求：\
- 当你回复 <continue> 时，正文至少包含以下内容：\
  1. 当前仍未覆盖或证据不足的 RFC 章节、模块、调用链、入口或状态流转\
  2. 为什么这里仍值得继续排查\
  3. 建议下一步优先怎么查\
- 当你回复 <complete> 时，正文至少包含以下内容：\
  1. 已经覆盖过的主要高风险 RFC 区域\
  2. 你为什么认为当前项目没有明显遗漏的不一致方向\
  3. 为什么现有未解决点不足以阻止结束本轮扫描\
- 当系统再次要求你继续工作时，你要根据上一轮指出的补查方向继续评估，而不是重新泛泛浏览整个项目。",
      "writable": false
    }
  ],
  "links": [
    {
      "from": "线索发现",
      "to": "疑点辩论",
      "trigger": "<continue>",
      "message_type": "last",
      "maxTriggerRounds": 999
    },
    { "from": "线索发现", "to": "线索完备性评估", "trigger": "<complete>", "message_type": "last" },
    { "from": "线索完备性评估", "to": "线索发现", "trigger": "<continue>", "message_type": "last", "maxTriggerRounds": 4 },
    { "from": "线索完备性评估", "to": "__end__", "trigger": "<complete>", "message_type": "none" }
  ]
}
